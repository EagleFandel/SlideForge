# Legacy Prototype Notice

`SlideForge/` is retained as a read-only legacy prototype reference.

## Scope

- This directory is not part of the active monorepo implementation.
- New features and fixes must be implemented under `apps/`, `packages/`, and `docs/`.

## Policy

- Do not evolve legacy runtime code in this folder.
- Keep only historical or protocol-reference material needed for migration context.
- Any new production-facing changes belong to the main workspace structure.
