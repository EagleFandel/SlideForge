# NPM configuration for SlideForge

# Use pnpm as package manager
engine-strict=true

# Ensure consistent installs
legacy-peer-deps=false

# Configure registry
registry=https://registry.npmjs.org/

# Configure access level for scoped packages
@slideforge:registry=https://registry.npmjs.org/
