// Types
export * from "./types";

// Validator
export { validate } from "./validator";
export type { ValidationError, ValidationResult } from "./validator";

// Utils
export { createSlideDocument, createSlide, createHeading, createText, createList } from "./utils";
